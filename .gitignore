# dependencies
node_modules/

# environment variables (local config: admin email, etc.)
.env
# generated from firestore.rules.template — run "npm run build:rules" to rebuild
firestore.rules

dist/
*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.swp

# keep the original single-file build during the split (remove in Phase 9)
!gantt.html

# project management notes
_pm/
