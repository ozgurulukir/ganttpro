# BACKLOG — GanttPro Security

Deferred / future / out-of-scope-for-now.

## Deferred

- Add `expires_at` / `created_at` to `gantt_shares` docs for token expiry
- Share revocation UI (delete share docs)
- Cloud Function for collab edits (currently cross-doc write rule is relaxed)
- `.env` indirection for Firebase config (currently hardcoded in `firebase.js`)
- Rate limiting on self-registration (`gantt_allowed_users`)
